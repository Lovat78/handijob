// src/hooks/useAutoLogin.ts\nimport { useEffect } from 'react';\nimport { useAuthStore } from '@/stores/authStore';\n\n/**\n * Hook d'auto-connexion pour faciliter les tests Pierre\n * Active uniquement en développement\n */\nexport const useAutoLogin = () => {\n  const { login, isAuthenticated, user } = useAuthStore();\n\n  useEffect(() => {\n    // Auto-login uniquement en dev et si pas déjà connecté\n    if (process.env.NODE_ENV === 'development' && !isAuthenticated && !user) {\n      console.log('🚀 Auto-login candidat pour Pierre...');\n      \n      // Délai pour éviter conflit initialisation\n      setTimeout(async () => {\n        try {\n          await login({\n            email: 'ahmed.benali@example.fr',\n            password: 'password123'\n          });\n          console.log('✅ Auto-login candidat réussi!');\n        } catch (error) {\n          console.log('❌ Auto-login échoué:', error);\n        }\n      }, 1000);\n    }\n  }, [login, isAuthenticated, user]);\n};